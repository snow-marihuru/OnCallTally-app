# OnCallTally 導入・利用手順

電話当番表から当番日数・手当支給額を自動集計し、Excelで出力するアプリです。以下の手順どおりに進めれば使えるようになります。

## 事前準備：Pythonのインストール（初回のみ）

すでにこのパソコンにPythonが入っている場合はこの手順は不要です。不明な場合は「アプリのセットアップ」に進み、エラーが出たらこちらに戻ってください。

1. ブラウザで https://www.python.org/downloads/ を開き、「Download Python」ボタンからインストーラーをダウンロードする
2. ダウンロードしたインストーラーを実行する
3. **最初の画面の下の方にある「Add python.exe to PATH」に必ずチェックを入れる**（これを忘れると後の手順でエラーになります）
4. 「Install Now」をクリックしてインストールを進める
5. インストール完了後、念のためパソコンを再起動する

## アプリのセットアップ（初回のみ）

1. 受け取ったフォルダ（または zip を展開してできたフォルダ）を、パソコンの好きな場所に置く（デスクトップなど）
2. フォルダの中の **`setup.bat`** をダブルクリックする
3. 黒い画面が開き、自動でセットアップが進む（数分かかることがある）
4. 「Setup complete.」と表示されたら完了。何かキーを押すとウィンドウが閉じる

### うまくいかない場合

- 「Python was not found」等のエラーが出る → 上の「事前準備」を行ってから、もう一度 `setup.bat` をダブルクリックする
- Pythonをインストールした直後の場合、パソコンを再起動してから試す

## アプリの起動（2回目以降、使うたびに）

1. フォルダの中の **`start_app.bat`** をダブルクリックする
2. 黒い画面が開き、数秒後にブラウザが自動で開く
3. ブラウザが自動で開かない場合は、ブラウザを手動で開いて次のアドレスを入力する

   ```
   http://127.0.0.1:5000
   ```

4. 使い終わったら、黒い画面（コンソール）を閉じるとアプリが終了する

## アプリの使い方

1. 担当者名・開始日・終了日を入力して「登録」する（人数分・期間分繰り返す）
2. 「集計開始日」「集計終了日」を指定し、「カレンダーで確認」を押すと当番カレンダーが表示される
   - 会社の公休日など、区分（平日／土曜／日祝日）を手動で直したい日は、カレンダー上のプルダウンから選び直す
3. 「Excelダウンロード」を押すと、担当者ごとの当番日数・手当支給額をまとめたExcelファイルがダウンロードされる

## 注意事項

- **入力したデータは保存されません。** 黒い画面（アプリ）を閉じたり、パソコンを再起動すると、それまでの入力内容は消えます。集計が終わったら、都度Excelファイルとして保存してください。
- Excelに出力される手当支給額は参考値です。実際の支給額は給与システム側で確定します。
